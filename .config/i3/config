# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:JetBrainsMono Nerd Font Mono 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# kill focused window
bindsym $mod+c kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+r mode "resize"

# borders and gaps
smart_borders on
for_window [floating] border pixel 5
for_window [tiling] border pixel 3
gaps outer 5
gaps inner 5
#smart_gaps on

# border colors
client.focused          #88C0D0 #285577 #ffffff #2e9ef4 #88C0D0
client.unfocused        #222222 #222222 #888888 #292d2e #434C5E
client.urgent           #BF616A #900000 #ffffff #BF616A #BF616A

# default floating windows
for_window [class="Ristretto"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="vlc"] floating enable
for_window [class="Xreader"] floating enable
for_window [title="^zoom$"] floating enable
for_window [class="zoom" title="Settings"] floating enable
for_window [title="Zoom Cloud Meetings"] floating enable
for_window [title="zoom"] floating enable
for_window [title="Zoom - Free Account"] floating disable
for_window [title="floating"] floating enable

floating_modifier $mod
bindsym $mod+Shift+f floating toggle

mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym $left       resize shrink width 10 px or 10 ppt
        bindsym $down       resize grow height 10 px or 10 ppt
        bindsym $up         resize shrink height 10 px or 10 ppt
        bindsym $right      resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left        resize shrink width 10 px or 10 ppt
        bindsym Down        resize grow height 10 px or 10 ppt
        bindsym Up          resize shrink height 10 px or 10 ppt
        bindsym Right       resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or Mod1+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym Mod1+r mode "default"
}

# workspace settings
# main apps
assign [class="zoom"] $ws1
assign [class="join$"] $ws1
# browsers
assign [class="Brave-browser-nightly"] $ws2
assign [class="Firefox"] $ws2
# social
assign [class="discord"] $ws3
assign [class="Caprine"] $ws3
assign [class="whatsdesk"] $ws3
assign [class="TelegramDesktop"] $ws3
assign [class="Signal"] $ws3
# media
assign [class="Gimp"] $ws4
assign [class="obs"] $ws4
assign [class="kdenlive"] $ws4
assign [class="Blender"] $ws4 
assign [class="Audacity"] $ws4 
assign [class="Inkscapep"] $ws4
assign [instance="libreoffice"] $ws4
assign [title="LibreOffice"] $ws4
assign [class="DesktopEditors"] $ws4

# dmenu configs
bindsym $mod+a exec j4-dmenu-desktop  --dmenu="dmenu -l 10 -p App:"
bindsym $mod+d exec dmenu_run -c -l 10 -p "Command:"
bindsym $mod+Ctrl+f exec dmenufm
bindsym $mod+x exec dmenu-pm -c -l 10
bindsym $mod+n exec networkmanager_dmenu -c -l 10
bindsym $mod+m exec ~/sh-scripts/dmenu-sh-scripts.sh

# Screenshots
bindsym $mod+Print exec --no-startup-id xfce4-screenshooter 

## Clipboard Screenshots
bindsym Ctrl+Print exec --no-startup-id maim | xclip -selection clipboard -t image/png
bindsym Ctrl+$mod+Print exec --no-startup-id maim --window $(xdotool getactivewindow) | xclip -selection clipboard -t image/png
bindsym Ctrl+Shift+Print exec --no-startup-id maim --select | xclip -selection clipboard -t image/png

# app shortcuts
bindsym Shift+Ctrl+S exec --no-startup-id xfce4-settings-manager
bindsym Shift+Ctrl+O exec --no-startup-id onlyoffice-desktopeditors
bindsym Shift+Ctrl+D exec --no-startup-id Discord
bindsym Shift+Ctrl+M exec --no-startup-id caprine
bindsym Shift+Ctrl+T exec --no-startup-id telegram-desktop
bindsym Shift+Ctrl+W exec --no-startup-id whatsdesk
bindsym Shift+Ctrl+F exec --no-startup-id thunar
bindsym Shift+Ctrl+Z exec --no-startup-id zoom
bindsym $mod+t exec ristretto /home/ronald/Desktop/timetable.png
bindsym $mod+Return exec xfce4-terminal
bindsym Ctrl+Return exec xfce4-terminal -T floating
bindsym $mod+Shift+Return exec brave-browser-nightly
bindsym $mod+Shift+Ctrl+Return exec firefox

# brightness control
bindsym XF86MonBrightnessDown exec xbacklight -dec 10
bindsym XF86MonBrightnessUp exec xbacklight -inc 10

# autostart script
exec_always nitrogen --restore
exec_always ibus-daemon --xim -d 
exec_always blueman-applet
exec_always nm-applet
exec_always dunst -config ~/.config/dunst/dunstrc
exec_always picom
exec_always source ~/.profile 
exec_always xbacklight -set 60
exec_always xfce4-panel
exec notify-send -i package_applications "$(xbps-install -M -n -u | wc -l) packages to be updated 📦"

# scratchpads
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show
for_window [instance="pavucontrol"] move scratchpad
# Google Meet Screenshare
for_window [title="meet.google.com is sharing your screen"] move scratchpad; exec notify-send -i "google-meet" "Google Meet is sharing your screen"
for_window [title="meet.google.com is sharing a window"] move scratchpad; exec notify-send -i "google-meet" "Google Meet is sharing your screen"

# polybar
#bar {
  # i3bar_command ~/.config/polybar/launch.sh
#}

# startup audio
# exec_always play -v 0.2 /home/ronald/Music/startup.mp3
